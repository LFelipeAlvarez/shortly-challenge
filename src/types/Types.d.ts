export type TFooterColumn = {
    header: string;
    rows: string[];
}


export type Card = {
    icon: string,
    title: string,
    description: string
}

export type Link = {
    normalLink: string;
    shortenedLink: string;
};